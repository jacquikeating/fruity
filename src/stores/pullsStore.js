import { create } from "zustand";

export const pullsStore = create((set) => ({
  /* 
    pullData,
    pullNumType,
    showEdit,
    updatePull,
    deletePull,
    progPhase,
    allowDelete,
  */
}));
